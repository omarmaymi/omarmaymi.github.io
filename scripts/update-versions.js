const fs=require("fs"),path=require("path"),crypto=require("crypto");function calculateFileHash(s){const e=fs.readFileSync(s);return crypto.createHash("md5").update(e).digest("hex").slice(0,8)}function updateVersions(){const s=path.join(__dirname,"..","version.json"),e=JSON.parse(fs.readFileSync(s,"utf8")),a=path.join(__dirname,"..");fs.readdirSync(a).filter((s=>s.endsWith(".css"))).forEach((s=>{const t=calculateFileHash(path.join(a,s)),i=path.basename(s,".css");e.assets.css[i]&&(e.assets.css[i]=t)})),fs.readdirSync(a).filter((s=>s.endsWith(".js"))).forEach((s=>{const t=calculateFileHash(path.join(a,s)),i=path.basename(s,".js");e.assets.js[i]&&(e.assets.js[i]=t)})),e.version=calculateFileHash(s),fs.writeFileSync(s,JSON.stringify(e,null,2)),console.log("Version file updated successfully")}updateVersions();