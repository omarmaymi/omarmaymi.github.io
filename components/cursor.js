const cursor=document.querySelector(".cursor"),cursorDot=document.querySelector(".cursor-dot");let mouseX=0,mouseY=0,cursorX=0,cursorY=0;function updateCursorPosition(o){mouseX=o.clientX,mouseY=o.clientY}function smoothCursor(){cursorX+=.2*(mouseX-cursorX),cursorY+=.2*(mouseY-cursorY),cursorDot.style.transform=`translate(${cursorX}px, ${cursorY}px)`,requestAnimationFrame(smoothCursor)}document.addEventListener("mousemove",updateCursorPosition),smoothCursor(),document.addEventListener("mouseleave",(()=>{cursor.style.opacity="0"})),document.addEventListener("mouseenter",(()=>{cursor.style.opacity="1"}));